<% unless current_user?(@user) %>
  <div id="follow_user_form">
    <% if current_user.following?(@user) %>
      <%= render 'users/follow_user_cancel' %>
    <% else %>
      <%= render 'users/follow_user' %>
    <% end %>
  </div>
<% end %>
