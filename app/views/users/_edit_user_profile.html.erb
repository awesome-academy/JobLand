<div class="popup-edit-user-contact">
  <button class=" btn open-button-edit-user-contact" onclick="openForm()">
    <%= t("userp.edit") %>
  </button>
  <div class="form-popup" id="profileForm-popup" style="display:none;">
    <div class="form-edit-user-contact">
      <%= form_for [ @user ]  do |f| %>
        <div class="form-edit-user-profile-left">
          <%= f.label "Phone" %>
          <%= f.text_field :phone, placeholder: 'Enter phone', class: 'form-control' %>
        </div>
        <div class="form-edit-user-profile-left">
          <%= f.label "Address" %>
          <%= f.text_field :address, placeholder: " Enter address", class: 'form-control' %>
        </div>
        <div class="form-edit-user-profile-center">
          <%= f.label :sex %><br />
          <label><%= t("job.male")%></label>
          <%= f.radio_button :sex, "Male", value: "1" %>
          <label><%= t("job.female")%></label>
          <%= f.radio_button :sex, "Female", value: "2" %>
        </div>
        <div class="form-edit-user-profile-center">
          <%= f.label "Birthday" %><br>
          <%= f.date_select :dob, order: [:month, :day, :year], prompt: { day: 'Select day', month: 'Select month', year: 'Select year' }, start_year: Date.today.year, end_year: Date.today.year - 150 %>
        </div>
        <div class = "submit-user-profile" >
          <button class="btn btn-danger cancel" type="button" onclick="closeForm()"><%= t("userp.cancel") %></button>
          <%= f.submit t('userp.update'), class: "btn btn-outline-secondary" %>
        </div>
      <% end %>
    </div><br>
  </div>
</div>

<script>
function openForm() {
    $("#profileForm-popup").show();
}
function closeForm() {
    $("#profileForm-popup").hide();
}
</script>
